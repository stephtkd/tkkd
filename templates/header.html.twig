<header>    <nav class="navbar navbar-dark bg-dark navbar-expand-lg">        <div class="container-fluid bd-highlight">            <a class="navbar-brand" href="{{ path('app_home') }}">                <img src="{{ asset('build/LogoRond.png') }}" alt="Logo Taekwonkido Phenix" width="50" height="auto" class="d-inline-block align-text-top">            </a>            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">                <span class="navbar-toggler-icon"></span>            </button>            <div class="navbar-collapse" id="navbarNav">                <ul class="navbar-nav me-auto bd-highlight">                    <li class="nav-item align-middle">                        <a class="nav-link text-white" href="{{ path('app_home') }}">Taekwonkido Phenix</a>                    </li>                    <li class="nav-item align-middle">                        <a class="nav-link text-white" href="{{ path('app_pictures') }}">Albums</a>                    </li>                    <li class="nav-item align-middle">                        <a class="nav-link text-white" target="_blank" href="https://shop.phenix.cenaclerm.fr/">Boutique</a>                    </li>                    <li class="nav-item align-middle">                        <a class="nav-link text-white" href="{{ path('app_contact') }}">Contact</a>                    </li>                </ul>                <div class="navbar-item nav-dropdown-menu  bd-highlight">                    <details>                        <summary>                            {% if app.user %}                                <a class="text-white" href="{{ path('app_account') }}">Mon compte <small>*{{ app.user.firstname }}*</small></a> | <a class="text-white" href="{{ path('app_logout') }}">Déconnexion</a>                            {% else %}                                <a class="text-white" href="{{ path('app_login')  }}">Connexion</a> | <a class="text-white" href="{{ path('app_register') }}">Inscription</a>                            {% endif %}                        </summary>                        <ul class="dropdown">                            <li><a class="nav-link text-white" href="{{ path('account_member') }}">Gérer mes adhérents</a></li>                            <li><a class="nav-link text-white" href="{{ path('app_subscribe_event') }}">Suivre vos évènements</a></li>                            <li><a class="nav-link text-white" href="{{ path('app_account_change_password') }}">Modifier mon profil</a></li>                            {% if is_granted('ROLE_ADMIN') %}                                <li><a href="{{ path('admin') }}">Dashboard Admin</a></li>                            {% endif %}                        </ul>                    </details>                </div>            </div>        </div>    </nav></header>